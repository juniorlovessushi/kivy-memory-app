# Kivy Memory App

A cross-platform Python application built with **Kivy** and **SQLite3**. The application provides persistent memory by storing interaction timestamps locally in an encrypted/relational database context.

## Setup Instructions

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/juniorlovessushi/kivy-memory-app.git](https://github.com/juniorlovessushi/kivy-memory-app.git)
   cd kivy-memory-app
